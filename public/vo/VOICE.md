# Placeholder voice

ep01's spoken track is **espeak-ng** (`en-us`, 120 wpm).

It is a **system TTS placeholder** so the cut has a spoken line to lock captions and scene length to.

- Not Perk
- Not a Perk clone
- Not a human host on camera

Cairn is the body. Replace these files with a chosen voice later. Do not clone Perk.

Regenerate:

```
npm run vo
```

Requires `espeak-ng` and `ffmpeg`.
